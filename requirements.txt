# Závislosti projektu conBond3.
#
# Prostředí se staví takto:
#
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Kontrolu, že je prostředí kompletní, dělá ./run-python.
#
# --------------------------------------------------------------------------
# Kód našich modulů závislosti NEMÁ a mít nesmí.
#
# service.py, api.py, client.py i control.py vystačí se standardní knihovnou
# (MODULES.md § 19). To, že v .venv něco leží, neznamená, že si to smí náš kód
# naimportovat — hranice vede po procesu, ne po prostředí.
#
# Závislosti níže patří vendorovaným nástrojům, které moduly PROVOZUJÍ jako
# vlastní proces vedle sebe.
# --------------------------------------------------------------------------

# --- cb-udpipe: vendorovaný UDPipe 2 -------------------------------------
# Odkomentováno při stavbě modulu cb-udpipe (2026-08-03).
# Verze jsou přišpendlené schválně — je to ověřená kombinace, na které rozbor
# běží. TensorFlow s transformers se umí rozejít a hledat to podruhé je
# zbytečná práce. (Převzato z conBond2, kde je to odzkoušené.)
#
tensorflow==2.21.0
tf_keras==2.21.0
transformers==4.49.0
ufal.udpipe==1.4.0.1
ufal.morphodita==1.11.3.3
ufal.chu_liu_edmonds==1.0.3

# --- cb-field: maticová reprezentace pole ---------------------------------
# Vědomá, zdůvodněná výjimka z pravidla „moduly bez závislostí" (§ 19),
# schváleno 2026-08-03: koše jsou matice vah (float32, −1…+1) a maticové
# počítání je přesně to, k čemu numpy je. Import je omezen na cb_field.
numpy==2.4.6

# viewBase2 (vizualizace znalostního grafu, krok I) se pořizuje ručně —
# git URL by rozbila kontrolu tvaru řádků výše:
#   .venv/bin/pip install 'viewbase @ git+https://github.com/alchy/viewBase2#subdirectory=python'
